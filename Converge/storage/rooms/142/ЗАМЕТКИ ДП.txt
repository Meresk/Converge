!СДЕЛАНО  0) одинаковые ники фикс -> использовать livekit sdk api на go

!СДЕЛАНО 0.1) комнаты с одинаковыми названиями

!СДЕЛАНО 1) ФИО для user

!СДЕЛАНО 2) У комнаты видно ФИО преподавателя

!СДЕЛАНО НО ЕСТЬ НЮАНС 3) редактирование и удаление комнат (в таблице)

!СДЕЛАНО 4) Таблица с ВСЕМИ комнатами только те которые создал преподаватель за которого мы зашли

5) файловый сторадж для комнаты (заметки? материалы?) чтобы мог только преподаватель закидывать (у скинутых файлов даты загрузки и возможность удалять)

!СДЕЛАНО 6) Писать в чат только для преподавателя

!СДЕЛАНО 7) Вместо ID, номер строки в таблицах у AdminPage и TeacherPage

!СДЕЛАНО 8) цвет на тексте кнопок

!СДЕЛАНО 9) редизайн RoomPage (перенести закрытие комнаты на teacherPage в таблицу)

!СДЕЛАНО 10) генерация пароля для комнаты и глазик для пароля

11) Поиск и фильтрация комнат для студентов. Решить - карточки или таблица (поле ввода, поиск по названию и фио одноврменно)

!СДЕЛАНО 12) Показ ников с модерацией -> использовать livekit sdk api на go

!СДЕЛАНО 13) при токене который заекспайриля мы все равно видим элементы управления в ControlComponent если зайти через студента

!СДЕЛАНО 14) Вынести в переменную url livekit сервера


жизенный цикл комнаты - комната на семестр



